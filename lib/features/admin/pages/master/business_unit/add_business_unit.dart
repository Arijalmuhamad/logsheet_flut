import 'package:flutter/material.dart';

class MasterAddCompany extends StatelessWidget {
  const MasterAddCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Tambah Company")),
      body: Center(child: const Text("Tambah Company Screen")),
    );
  }
}
